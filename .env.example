# PostgreSQL Database URL
# For local development with Docker:
# DATABASE_URL=postgresql://postgres:password@localhost:5432/placement_db
# 
# You can start the PostgreSQL container with:
# docker compose up -d

DATABASE_URL=postgresql://postgres:password@localhost:5432/placement_db
